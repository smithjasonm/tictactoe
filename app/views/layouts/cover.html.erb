<!DOCTYPE html>
<html id="cover-page"> <!-- Note: ID is referenced in script at end of body. -->
  <head>
    <title>Tic-Tac-Toe</title>
    <%= render 'layouts/head' %>
  </head>
  <body data-no-turbolink>
    <div class="site-wrapper">

      <div class="site-wrapper-inner">

        <div class="cover-container">

          <div class="masthead clearfix">
            <div class="inner">
              <h3 class="masthead-brand">Tic-Tac-Toe</h3>
              <nav>
                <ul class="nav masthead-nav">
                  <%= nav_link 'Home', root_path %>
                  <%= nav_link 'Features', '#' %>
                  <%= nav_link 'Contact', '#' %>
                  <%= nav_link 'Login', login_path %>
                </ul>
              </nav>
            </div>
          </div>

          <div class="inner cover">
            <%= render 'layouts/flash' %>
            <%= yield %>
          </div>

          <div class="mastfoot">
            <div class="inner">
              <p>© <%= Date.today.year %> JMS</p>
            </div>
          </div>

        </div>

      </div>

    </div>
    
    <script>
      // Must reset document ID, as Turbolinks will not update it.
      document.documentElement.id = "cover-page";
    </script>
  </body>
</html>